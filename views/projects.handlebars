<div class="project-card">
    {{#each workspaces}}
    <div class="project">
        <h2>{{name}}</h2>
        {{#each projects}}
        <h3>{{name}}</h3>
        {{#each tasks}}
        <p>{{title}}</p>
        <p>{{contents}}</p>
        {{/each}}
        {{/each}}
    </div>
    {{/each}}
</div>